# 61. Найти произведение двух матриц

from random import randint
n, m = 3, 6
a = [[randint(1, 10) for j in range(m)] for i in range(n)]


import function as aa
aa.print_matrix(a)

# 59. В прямоугольной матрице найти строку с наименьшей суммой элементов.
def Task59 (array):
    minsum=0
    a1=0
    sum1=0

    [[sum1=sum1+a[i][j] for j in range(m)] for i in range(n)]
   
    for for j in range(len(array))
    
        for (int j = 0; j < a.GetLength(1); j++)
        {
            sum=sum+a[i,j]; System.Console.WriteLine(sum);
            if (sum<minsum) {minsum=sum; a1=i;}
    
        //sum=0;
    



# # def SetArray2D(array):
#     l = [[randint(1, 10) for j in range(5)] for i in range(7)]
#     print (l)

# exit()

#     Random rnd = Random().Next(1,20);
#     for (int i = 0; i < a.GetLength(0); i++)
#     {
#         for (int j = 0; j < a.GetLength(1); j++)
#         {
#             a[i, j] = rnd.Next(1, 20);
#         }
#     }


# def Print2D (array):

# {
#     for (int i = 0; i < a.GetLength(0); i++)
#     {
#         for (int j = 0; j < a.GetLength(1); j++)
#         {
#             Console.Write($"{a[i, j],4}");
#         }
#         Console.WriteLine();
#     }
# }

# void Task61(int[,] a, out int minsum, out int a1) 
# {
#     int mimsum=0;
#     a1=0;
   
#     for (int i = 0; i < a.GetLength(0); i++)
#     {
#         sumstr=0;
#         for (int j = 0; j < a.GetLength(1); j++)
#         {
#             c[i,j] = sumstr+a[i,j]*b[i,j];
#             sum=sum+a[i,j]; System.Console.WriteLine(sum);
#             if (sum<minsum) {minsum=sum; a1=i;}
#     }
#         //sum=0;
#     }

# }

# int m = 5;
# int n = 5;
# int k=1;
# int[,] a;
# a = new int[m, n];
# b = new int[n, k];
# c = new int[m, k];

# int summin=0, a1=0;
# SetArray2D(a);
# SetArray2D(b);

# Print(a);
# Console.WriteLine();
# Print (b);
# Console.WriteLine();

# Task59(a, out summin, out a1); 
# System.Console.WriteLine($"мин сумма {summin} в строчке {a1}"); 
# Print(a);






